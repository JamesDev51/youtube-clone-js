extends layouts/main

block content
    .form-container
        span.form-title 비밀번호변경
        form(action=`/users${routes.changePassword}`, method="post")
            label(for="name") 현재 비밀번호
            input(type="password", name="password", placeholder="")
            label(for="name") 신규 비밀번호
            input(type="password", name="password1", placeholder="신규 비밀번호")
            input(type="password", name="password2", placeholder="신규 비밀번호 재입력")
            input(type="submit", value="변경하기")